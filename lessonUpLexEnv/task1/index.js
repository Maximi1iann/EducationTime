const message = 'Have a good day!';

function sendMessage(name) {
    let greeting = 'Hi, ';

    if (name) {
        const greeting = 'Hello, '
        const name = 'Tad';
        console.log(greeting + name);
    } else {
        console.log(message);
    }
}

sendMessage('Ann');